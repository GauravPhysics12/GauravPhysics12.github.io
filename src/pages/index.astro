---
import { Container } from '@components/odyssey-theme';
import Layout from '../layouts/Page.astro';
import LiveSkyChart from '../components/LiveSkyChart.astro';

import jwstImage from '../assets/images/Home/ripple.gif';
import codeImage from '../assets/images/Home/code.jpg';
import notesImage from '../assets/images/Home/notes.jpg';
import astroImage from '../assets/images/Home/astro1.jpg';
import blogImage from '../assets/images/Home/blog.png';
---

<Layout>
	<section class="hero">
		<div class="hero__glow" aria-hidden="true"></div>
		<Container>
			<div class="hero__content">
				<p class="hero__eyebrow">Physics &middot; Astrophotography &middot; Curiosity</p>
				<h1>Gaurav Rachh</h1>
				<p class="hero__tagline">"Curious human"</p>
			</div>
		</Container>
	</section>

	<section class="intro-section">
		<Container>
			<p>
				Hello! I'm a final-year physics student at IISc, and this website is a map of my curiosity. It charts a course from my academic work in astrophysics and gravitational physics to my passions for astrophotography, music, and deep philosophical questions. Whether you're here for the science, the art, or a bit of both, I invite you to explore.
			</p>
		</Container>
	</section>

	<Container>
		<section class="split-section">
			<div class="split-section__text">
				<h2>My Projects</h2>
				<p>
					My work ranges from deep theory to computational physics. I've worked on a theory for observing reheated neutron stars, simulated high-energy particle collisions using MadGraph and Pythia, and explored black hole quasi-normal modes. My current master's thesis is focused on Black Hole Perturbation Theory.
				</p>
				<a href="/projects" class="button">View All Projects</a>
			</div>
			<div class="split-section__image">
				<img src={jwstImage.src} alt="An image related to the James Webb Space Telescope" />
			</div>
		</section>

		<section class="split-section reverse">
			<div class="split-section__text">
				<h2>Coding & Software</h2>
				<p>
					From numerical simulations to introductory scripts for beginners, this section contains my project codes and tutorials for those new to coding in physics. You'll find Mathematica notebooks, Python scripts, and more.
				</p>
				<a href="/codes" class="button">Explore Codes</a>
			</div>
			<div class="split-section__image">
				<img src={codeImage.src} alt="A screenshot of physics code" />
			</div>
		</section>

		<section class="split-section">
			<div class="split-section__text">
				<h2>Physics Notes</h2>
				<p>
					Dive into my personal library of physics notes. This collection ranges from deep dives into core subjects like General Relativity to short articles on fascinating, off-the-beaten-path topics that have captured my curiosity.
				</p>
				<a href="/physics-notes" class="button">See All Notes</a>
			</div>
			<div class="split-section__image">
				<img src={notesImage.src} alt="A chalkboard with physics equations" />
			</div>
		</section>

		<section class="split-section reverse">
			<div class="split-section__text">
				<h2>Astrophotography</h2>
				<p>
					Beyond research, I'm passionate about capturing the beauty of the night sky. My gallery showcases celestial objects, and you'll also find tutorials on image processing with software like Siril.
				</p>
				<a href="/gallery" class="button">View Gallery</a>
			</div>
			<div class="split-section__image">
				<img src={astroImage.src} alt="A colorful nebula captured with a telescope" />
			</div>
		</section>

		<section class="split-section">
			<div class="split-section__text">
				<h2>Blog & Ideas</h2>
				<p>
					I believe in making complex scientific concepts accessible. In my blog, I break down fascinating topics in physics and share my thoughts on interesting thought experiments and new discoveries.
				</p>
				<a href="/blog" class="button">Explore Writings</a>
			</div>
			<div class="split-section__image">
				<img src={blogImage.src} alt="Doodle of people talking for blog" />
			</div>
		</section>


	</Container>

	<div class="feynman-quote">
		<span class="feynman-quote__mark" aria-hidden="true">&ldquo;</span>
		<p>The first principle is that you must not fool yourself and you are the easiest person to fool.</p>
		<p class="author">— Richard Feynman</p>
	</div>
</Layout>

<style>
	/* ---------- Hero ---------- */
	.hero {
		position: relative;
		padding: 5rem 1rem 4rem;
		text-align: center;
		overflow: hidden;
	}
	.hero__glow {
		position: absolute;
		inset: -20% -10% auto -10%;
		height: 32rem;
		background:
			radial-gradient(45% 60% at 30% 20%, var(--theme-glow-cyan), transparent 70%),
			radial-gradient(35% 50% at 75% 30%, var(--theme-glow-gold), transparent 70%);
		filter: blur(20px);
		pointer-events: none;
		z-index: 0;
	}
	.hero__content {
		position: relative;
		z-index: 1;
	}
	.hero__eyebrow {
		text-transform: uppercase;
		letter-spacing: 0.15em;
		font-size: var(--font-size-sm);
		color: var(--theme-primary);
		margin-bottom: 1rem;
	}
	.hero h1 {
		font-size: clamp(2.5rem, 6vw, 4rem);
		margin-bottom: 0.75rem;
		background: linear-gradient(135deg, var(--theme-on-bg) 30%, var(--theme-primary) 100%);
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
	}
	.hero__tagline {
		font-style: italic;
		font-size: 1.15rem;
		color: var(--theme-on-bg-muted);
		max-width: 600px;
		margin: auto;
	}

	/* ---------- Intro ---------- */
	.intro-section {
		padding: 1rem 1rem 3rem;
		text-align: center;
	}
	.intro-section p {
		max-width: 760px;
		margin: auto;
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--theme-on-bg-muted);
	}

	/* ---------- Split sections as glass cards ---------- */
	.split-section {
		display: grid;
		grid-template-columns: 1fr;
		gap: 2rem;
		align-items: center;
		margin: 3rem auto;
		padding: 2rem;
		border-radius: var(--theme-shape-radius);
		background: linear-gradient(160deg, var(--theme-surface-1), var(--theme-bg-elevated));
		border: 1px solid var(--theme-border);
		transition: border-color var(--theme-transition), transform var(--theme-transition), box-shadow var(--theme-transition);
	}
	.split-section:hover {
		border-color: rgba(127, 216, 255, 0.35);
		transform: translateY(-4px);
		box-shadow: 0 20px 40px -20px rgba(0, 0, 0, 0.6);
	}
	.split-section.reverse .split-section__image {
		order: -1;
	}
	.split-section__text h2 {
		font-size: 1.75rem;
		margin-top: 0;
		margin-bottom: 1rem;
		color: var(--theme-on-bg);
	}
	.split-section__text p {
		font-size: 1rem;
		line-height: 1.75;
		color: var(--theme-on-bg-muted);
		margin-bottom: 1.5rem;
	}
	.split-section__image img {
		width: 100%;
		height: auto;
		border-radius: calc(var(--theme-shape-radius) - 0.4rem);
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
	}
	.button {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		background-color: var(--theme-accent);
		color: var(--theme-on-primary);
		padding: 0.7rem 1.4rem;
		text-decoration: none;
		border-radius: var(--theme-button-border-radius);
		font-weight: 700;
		font-size: var(--font-size-sm);
		transition: background-color var(--theme-transition), transform var(--theme-transition);
	}
	.button:hover {
		background-color: var(--theme-accent-hover);
		transform: translateX(2px);
	}

	/* ---------- Feynman quote ---------- */
	.feynman-quote {
		position: relative;
		margin: 5rem auto;
		padding: 3rem 1.5rem;
		max-width: 720px;
		text-align: center;
	}
	.feynman-quote__mark {
		display: block;
		font-family: var(--theme-font-family-serif);
		font-size: 5rem;
		line-height: 1;
		color: var(--theme-highlight);
		opacity: 0.5;
		margin-bottom: -1rem;
	}
	.feynman-quote p:not(.author) {
		font-style: italic;
		font-size: 1.3rem;
		line-height: 1.6;
		color: var(--theme-on-bg);
		font-family: var(--theme-font-family-serif);
	}
	.feynman-quote .author {
		font-style: normal;
		font-weight: 700;
		margin-top: 1.25rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		font-size: var(--font-size-sm);
		color: var(--theme-primary);
	}

	/* Desktop Overrides */
	@media (min-width: 768px) {
		.hero {
			padding: 8rem 1rem 6rem;
		}
		.split-section {
			grid-template-columns: repeat(2, 1fr);
			gap: 3.5rem;
			margin: 4rem auto;
			padding: 3rem;
		}
		.split-section__text h2 {
			font-size: 2.25rem;
		}
	}
</style>